# Deliberate fake RSA key committed 2025-11-05 to test the GitLeaks CI job
# ("GITLEAKS TEST" comment in the commit; removed by the next commit, 38c0757).
# The value is the literal string THISISAFANTASTICALLYFAKETESTKEYFOR... — not a secret.
5fd0bfcf571dc74b4832c15d0cb65af696ba3cff:archivebox.Dockerfile:private-key:5

# False positive, 2026-07-27. A prose COMMENT in capture.lib.sh explaining why the
# triage stopped sourcing the whole .env happened to name two config variables next
# to the word "password"; gitleaks 8.24.3's generic-api-key rule scores that as a
# finding. No value is present — only variable names. The comment was reworded in
# the following commit, but the phrasing is permanent in this one, so a full-history
# scan needs this line. (Note 8.30.1 does NOT flag it — scan with the version CI
# pins, not latest, or you will not reproduce it.)
ae6f3cd8036ee0947d276557a327e5e77bd5be9a:capture/scripts/capture.lib.sh:generic-api-key:230
