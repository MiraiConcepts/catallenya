services:
  memoka:
    container_name: memoka
    image: ghcr.io/carrein/memoka:latest
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 256M
    environment:
      # Passwords
      SERVERPOD_PASSWORD_database: ${MEMOKA_DB_PASSWORD}
      SERVERPOD_PASSWORD_redis: ${MEMOKA_REDIS_PASSWORD}
      # Public URL configuration (override production.yaml defaults)
      SERVERPOD_apiServer_publicHost: "${TAILNET_DOMAIN}.${TAILNET_DNS_NAME}"
      SERVERPOD_apiServer_publicPort: "${MEMOKA_REVERSE_PROXY_PORT}"
      SERVERPOD_webServer_publicHost: "${TAILNET_DOMAIN}.${TAILNET_DNS_NAME}"
      SERVERPOD_webServer_publicPort: "${MEMOKA_REVERSE_PROXY_PORT}"
    depends_on:
      memoka_postgresql:
        condition: service_healthy
      memoka_redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    volumes:
        - memoka_media_data:/app/data/media
    labels:
      - flame.type=application
      - flame.name=Memoka
      - flame.icon=custom
      - com.centurylinklabs.watchtower.enable=true

  memoka_postgresql:
    container_name: memoka_postgresql
    hostname: postgresql
    image: pgvector/pgvector:pg17
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - FOWNER
      - DAC_OVERRIDE
    deploy:
      resources:
        limits:
          memory: 256M
    environment:
      POSTGRES_USER: memoka_user
      POSTGRES_PASSWORD: ${MEMOKA_DB_PASSWORD}
      POSTGRES_DB: memoka
    volumes:
      - memoka_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U memoka_user -d memoka"]
      interval: 10s
      timeout: 5s
      retries: 5

  memoka_redis:
    container_name: memoka_redis
    image: redis:7-alpine
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - SETUID
      - SETGID
    deploy:
      resources:
        limits:
          memory: 64M
    networks:
      default:
        aliases:
          - redis
    command: redis-server --requirepass ${MEMOKA_REDIS_PASSWORD}
    volumes:
      - memoka_redis_data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a $${MEMOKA_REDIS_PASSWORD} ping || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  tailscale:
    # This needs to be set to 'catallenya' as the certificates generated are associated with this name.
    hostname: catallenya
    container_name: tailscaled
    image: tailscale/tailscale:latest
    restart: unless-stopped
    user: "1000:1000" # Run as host user UID:GID so we can read/write files created by container on shared volumes.
    network_mode: host
    deploy:
      resources:
        limits:
          memory: 256M
    cap_drop:
      - ALL
    cap_add:
      - NET_ADMIN
      - NET_RAW
      - NET_BIND_SERVICE
    volumes:
      - /dev/net/tun:/dev/net/tun
      - ${ZPOOL_VOLUME}/tailscale/data:/var/lib
      # Adds volume for the tailscaled.sock so its present on the host system where caddy communicates with tailscale. See: https://github.com/tailscale/tailscale/issues/6849.
      - ${ZPOOL_VOLUME}/tailscale/tmp:/tmp
    environment:
      # Persist the state in host and survives container reboot. See: https://github.com/tailscale/tailscale/issues/4913#issuecomment-1186402307.
      - TS_STATE_DIR=/var/lib/tailscale
      # Use TS_AUTH_KEY on initial boot. This key will be persisted in var/lib/tailscale and can be removed on subsequent runs.
    labels:
      - com.centurylinklabs.watchtower.enable=true

  cloudflared:
    container_name: cloudflared
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    user: "1000:1000"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 128M
    command: tunnel --credentials-file /run/secrets/cloudflared-secret run catallenya-tunnel
    secrets:
      - cloudflared-secret
    labels:
      - com.centurylinklabs.watchtower.enable=true

  caddy:
    container_name: caddy
    image: caddy:latest
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    deploy:
      resources:
        limits:
          memory: 128M
    depends_on:
      - tailscale
    environment:
      TAILNET_DOMAIN: ${TAILNET_DOMAIN}
      TAILNET_DNS_NAME: ${TAILNET_DNS_NAME}
      FLAME_REVERSE_PROXY_PORT: ${FLAME_REVERSE_PROXY_PORT}
      SYNCTHING_REVERSE_PROXY_PORT: ${SYNCTHING_REVERSE_PROXY_PORT}
      IMMICH_REVERSE_PROXY_PORT: ${IMMICH_REVERSE_PROXY_PORT}
      RADICALE_REVERSE_PROXY_PORT: ${RADICALE_REVERSE_PROXY_PORT}
      GHOST_REVERSE_PROXY_CADDY_PORT: ${GHOST_REVERSE_PROXY_CADDY_PORT}
      NTFY_REVERSE_PROXY_PORT: ${NTFY_REVERSE_PROXY_PORT}
      ARCHIVEBOX_REVERSE_PROXY_PORT: ${ARCHIVEBOX_REVERSE_PROXY_PORT}
      MEMOKA_REVERSE_PROXY_PORT: ${MEMOKA_REVERSE_PROXY_PORT}
    ports:
      - ${GHOST_REVERSE_PROXY_CADDY_PORT}:${GHOST_REVERSE_PROXY_CADDY_PORT}
      - ${FLAME_REVERSE_PROXY_PORT}:${FLAME_REVERSE_PROXY_PORT}
      - ${SYNCTHING_REVERSE_PROXY_PORT}:${SYNCTHING_REVERSE_PROXY_PORT}
      - ${IMMICH_REVERSE_PROXY_PORT}:${IMMICH_REVERSE_PROXY_PORT}
      - ${RADICALE_REVERSE_PROXY_PORT}:${RADICALE_REVERSE_PROXY_PORT}
      - ${NTFY_REVERSE_PROXY_PORT}:${NTFY_REVERSE_PROXY_PORT}
      - ${ARCHIVEBOX_REVERSE_PROXY_PORT}:${ARCHIVEBOX_REVERSE_PROXY_PORT}
      - ${MEMOKA_REVERSE_PROXY_PORT}:${MEMOKA_REVERSE_PROXY_PORT}  # API server port
    volumes:
      - ${ZPOOL_VOLUME}/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ${ZPOOL_VOLUME}/tailscale/tmp/tailscaled.sock:/var/run/tailscale/tailscaled.sock:ro
      - caddy-data:/data # Persistent volume for certificates.
      - caddy-config:/config # Persistent volume for config cache.
    labels:
      - com.centurylinklabs.watchtower.enable=true

  immich-server:
    container_name: immich-server
    image: ghcr.io/immich-app/immich-server:release
    restart: unless-stopped
    user: "1000:1000"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 1G
    ports:
    - "127.0.0.1:2283:2283" # Allow expose immich server over localhost directly when on same wi-fi network.
    volumes:
      - ${IMMICH_VOLUME}:/usr/src/app/upload
      - /etc/localtime:/etc/localtime:ro
    secrets:
      - postgres-secret
    environment:
      DB_HOSTNAME: ${DB_HOSTNAME}
      DB_USERNAME: ${DB_USERNAME}
      DB_DATABASE_NAME: ${DB_DATABASE_NAME}
      DB_PASSWORD_FILE: /run/secrets/postgres-secret
      REDIS_HOSTNAME: immich_redis
    depends_on:
      - immich_redis
      - postgres
    healthcheck:
      disable: false
    labels:
      - flame.type=application
      - flame.name=immich
      - flame.url=${IMMICH_URL}
      - flame.icon=custom

  immich-machine-learning:
    container_name: immich-machine-learning
    image: ghcr.io/immich-app/immich-machine-learning:release
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 2G
    volumes:
      - model-cache:/cache
    healthcheck:
      disable: false

  immich_redis:
    container_name: immich-redis
    image: docker.io/valkey/valkey:8@sha256:81db6d39e1bba3b3ff32bd3a1b19a6d69690f94a3954ec131277b9a26b95b3aa
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - SETUID
      - SETGID
    deploy:
      resources:
        limits:
          memory: 64M
    healthcheck:
      test: redis-cli ping || exit 1
    labels:
      - com.centurylinklabs.watchtower.enable=true

  postgres:
    container_name: immich-postgres
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0@sha256:bcf63357191b76a916ae5eb93464d65c07511da41e3bf7a8416db519b40b1c23
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - FOWNER
      - DAC_OVERRIDE
    deploy:
      resources:
        limits:
          memory: 512M
    secrets:
      - postgres-secret
    environment:
      POSTGRES_HOST: ${DB_HOSTNAME}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres-secret
      POSTGRES_DB: ${DB_DATABASE_NAME}
      POSTGRES_INITDB_ARGS: '--data-checksums'
    volumes:
      - ${DB_VOLUME}:/var/lib/postgresql/data
    shm_size: 128mb

  ghost:
    container_name: ghost
    image: ghost:latest
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - SETUID
      - SETGID
    deploy:
      resources:
        limits:
          memory: 512M
    environment:
      - database__client=sqlite3
      - database__connection__filename=/var/lib/ghost/content/data/ghost.db
      - url=${PUBLIC_URL}
    volumes:
      - ${GHOST_VOLUME}:/var/lib/ghost/content
    labels:
      - flame.type=application
      - flame.name=ghost
      - flame.url=${GHOST_URL}
      - flame.icon=custom
      - com.centurylinklabs.watchtower.enable=true

  flame:
    container_name: flame
    image: pawelmalak/flame:latest
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - FOWNER
      - DAC_OVERRIDE
    deploy:
      resources:
        limits:
          memory: 256M
    volumes:
      - ${FLAME_VOLUME}:/app/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
    secrets:
      - password
    environment:
      - PASSWORD_FILE=/run/secrets/password
    labels:
      - com.centurylinklabs.watchtower.enable=true

  syncthing:
    container_name: syncthing
    image: syncthing/syncthing:latest
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
    deploy:
      resources:
        limits:
          memory: 512M
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - ${SYNCTHING_VOLUME}:/var/syncthing
    labels:
      - flame.type=application
      - flame.name=syncthing
      - flame.url=${SYNCTHING_URL}
      - flame.icon=custom
      - com.centurylinklabs.watchtower.enable=true

  radicale:
    container_name: radicale
    image: tomsquest/docker-radicale:latest
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - SETUID
      - SETGID
      - CHOWN
      - KILL
    deploy:
      resources:
        limits:
          memory: 256M
          pids: 50
    volumes:
      - ${RADICALE_DATE_VOLUME}:/data
      - ${RADICALE_CONFIG_VOLUME}:/config:ro
    labels:
      - flame.type=application
      - flame.name=radicale
      - flame.url=${RADICALE_URL}
      - flame.icon=custom
      - com.centurylinklabs.watchtower.enable=true

  ntfy:
    container_name: ntfy
    image: binwiederhier/ntfy
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 128M
    command:
      - serve
    environment:
      - TZ=Asia/Singapore
    volumes:
      - ntfy-cache:/var/cache/ntfy
      - ntfy:/etc/ntfy
    labels:
      - flame.type=application
      - flame.name=ntfy
      - flame.url=${NTFY_URL}
      - flame.icon=custom
      - com.centurylinklabs.watchtower.enable=true

  memos:
    container_name: memos
    image: neosmemo/memos:stable
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - SETUID
      - SETGID
    deploy:
      resources:
        limits:
          memory: 128M
    volumes:
      - ${MEMOS_VOLUME}:/var/opt/memos
    environment:
      - MEMOS_MODE=prod
    labels:
      - flame.type=application
      - flame.name=memos
      - flame.url=${MEMOS_URL}
      - flame.icon=custom
      - com.centurylinklabs.watchtower.enable=true

  archivebox:
    container_name: archivebox
    build:
      context: .
      dockerfile: archivebox.Dockerfile # Custom Dockerfile for ArchiveBox.
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - FOWNER
      - DAC_OVERRIDE
    deploy:
      resources:
        limits:
          memory: 256M
    volumes:
      - ${ARCHIVEBOX_VOLUME}:/data
    environment:
      - ALLOWED_HOSTS=${TAILNET_DOMAIN}.${TAILNET_DNS_NAME}
      - PUBLIC_INDEX=False # Disable public listing.
      - PUBLIC_SNAPSHOTS=False # Disable public snapshot access.
      - PUBLIC_ADD_VIEW=True # Enable anonymous link submissions.
      - SEARCH_BACKEND_ENGINE=sonic # Search backend (uses Sonic container below).
      - SEARCH_BACKEND_HOST_NAME=archivebox_sonic
      - SEARCH_BACKEND_PASSWORD=/run/secrets/sonic-secret
      - SAVE_ARCHIVE_DOT_ORG=False # Don't submit to archive.org.
      - MAX_MEDIA_SIZE=5000m # Allow up to 5 GB media (YouTube videos, etc.).
      - TIMEOUT=120 # Allow slow pages up to 120s.
      - CHECK_SSL_VALIDITY=False
    labels:
      - flame.type=application
      - flame.name=archivebox
      - flame.url=${ARCHIVEBOX_URL}
      - flame.icon=custom
    # No Watchtower pull as we use a local Dockerfile instead of a registry reference.

  archivebox_scheduler:
    container_name: archivebox_scheduler
    image: archivebox/archivebox:latest
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - FOWNER
      - DAC_OVERRIDE
    deploy:
      resources:
        limits:
          memory: 256M
    command: schedule --foreground --update --every=day
    volumes:
      - ${ARCHIVEBOX_VOLUME}:/data
    environment:
      - SEARCH_BACKEND_ENGINE=sonic
      - SEARCH_BACKEND_HOST_NAME=archivebox_sonic
      - SEARCH_BACKEND_PASSWORD=/run/secrets/sonic-secret
      - TIMEOUT=120
    secrets:
      - sonic-secret
    depends_on:
      - archivebox
      - archivebox_sonic
    labels:
      - com.centurylinklabs.watchtower.enable=true

  archivebox_sonic:
    container_name: archivebox_sonic
    image: archivebox/sonic:latest
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 64M
    environment:
      - SEARCH_BACKEND_PASSWORD=/run/secrets/sonic-secret
    secrets:
      - sonic-secret
    volumes:
      - ${SONIC_VOLUME}:/var/lib/sonic/store
    labels:
      - com.centurylinklabs.watchtower.enable=true

  watchtower:
    container_name: watchtower
    image: nickfedor/watchtower:latest
    restart: unless-stopped
    # Note: No cap_drop/security_opt — Watchtower needs full Docker socket
    # access for self-update and container lifecycle management. Hardening
    # breaks its self-update pull and prevents scanning other containers.
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ${ZPOOL_VOLUME}/watchtower/config.json:/config.json:ro
    environment:
      - WATCHTOWER_LABEL_ENABLE=true
      - WATCHTOWER_NOTIFICATIONS=shoutrrr
      - WATCHTOWER_NOTIFICATION_URL=${NTFY_PROTOCOL_URL}/${WATCHTOWER_NTFY_TOPIC}
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_REMOVE_VOLUMES=true
      - WATCHTOWER_POLL_INTERVAL=3600
    labels:
      - com.centurylinklabs.watchtower.enable=true

  carrein-blog:
    container_name: carrein-blog
    image: ghcr.io/carrein/carrein-blog:latest
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
    deploy:
      resources:
        limits:
          memory: 128M
    labels:
      - flame.type=application
      - flame.name=carrein-blog
      - flame.url=${CARREIN_BLOG_URL}
      - flame.icon=custom
      - com.centurylinklabs.watchtower.enable=true

  zipline_postgresql:
    container_name: zipline-postgres
    image: postgres:16
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - FOWNER
      - DAC_OVERRIDE
    deploy:
      resources:
        limits:
          memory: 256M
    environment:
      POSTGRES_USER: ${POSTGRESQL_USER}
      POSTGRES_DB: ${POSTGRESQL_DB}
      POSTGRES_PASSWORD: ${POSTGRESQL_PASSWORD}
    volumes:
      - ${POSTGRESQL_VOLUME}:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD', 'pg_isready', '-U', 'zipline']
      interval: 10s
      timeout: 5s
      retries: 5

  zipline:
    container_name: zipline
    image: ghcr.io/diced/zipline:latest
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 512M
    environment:
      - DATABASE_URL=postgres://${POSTGRESQL_USER}:${POSTGRESQL_PASSWORD}@zipline_postgresql:5432/${POSTGRESQL_DB}
      - CORE_SECRET=${ZIPLINE_CORE_SECRET}
    depends_on:
      zipline_postgresql:
        condition: service_healthy
    volumes:
      - ${ZIPLINE_VOLUME}/uploads:/zipline/uploads
      - ${ZIPLINE_VOLUME}/public:/zipline/public
      - ${ZIPLINE_VOLUME}/themes:/zipline/themes
    healthcheck:
      test: ['CMD', 'wget', '-q', '--spider', 'http://127.0.0.1:3000/api/healthcheck']
      interval: 15s
      timeout: 2s
      retries: 2
    labels:
      - flame.type=application
      - flame.name=zipline
      - flame.url=${ZIPLINE_URL}
      - flame.icon=custom
      - com.centurylinklabs.watchtower.enable=true

secrets:
  password: # Flame hardcodes the password field.
    file: flame/password.txt
  cloudflared-secret:
    file: cloudflared/token.json
  sonic-secret:
    file: archivebox/password.txt
  postgres-secret:
    file: postgres/password.txt

volumes:
  model-cache:
  ntfy:
  ntfy-cache:
  caddy-data:
  caddy-config:
  memoka_postgres_data:
  memoka_redis_data:
  memoka_media_data:
